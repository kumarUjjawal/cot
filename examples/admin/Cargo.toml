[package]
name = "example-admin"
version = "0.1.0"
publish = false
description = "Admin panel - Cot example."
edition = "2021"

[dependencies]
cot = { path = "../../cot", features = ["live-reload"] }
rinja = "0.3.5"
